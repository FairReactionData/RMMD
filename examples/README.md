The examples in this directory do not contain real data, but are meant to illustrate the structure of an RMMD file and how to use the schema.
Therefore, other files referenced inside of these example files (e.g. `./CITATION.cff`) do not exist.
Furthermore, the examples do not show all possible fields of the schema, but only a subset of them.